== render 'common/breadcrumb', 
  links:        [:root], 
  current_page: "「#{@series.title}」シリーズ"

.articles
  - @articles.each do |article|
    = render 'articles/article_line', article: article

= paginate @articles